# 监控服务端配置
server:
  port: 8080

spring:
  application:
    name: monitor-server

monitor:
  server:
    # 推送间隔（毫秒）
    push-interval: 3000
    # 客户端超时判定时间（毫秒），超过此时间未收到数据则判定离线
    client-timeout: 30000

logging:
  level:
    com.monitor: DEBUG
